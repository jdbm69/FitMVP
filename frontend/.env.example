# Backend base URL (no trailing slash).
# Local dev: point at the API on the host.
# Docker (web image): usually /api (same origin; Nginx proxies to the api service).
VITE_API_URL=http://localhost:3001/api
